-content_for :title do
  =@order
#breadcrumb
  =link_to "Dashboard", admin_path
  =">"
  =link_to "Orders", admin_orders_path
  =">"
  =link_to @order, admin_order_path(@order)
%h1= @order
=render "show", :order => @order
%hr
=form_tag cancel_admin_order_path(@order), :class => 'formtastic' do
  %fieldset.inputs
    %ol
      %li
        =label_tag :reason
        =text_field_tag :reason
  %fieldset.buttons
    %ol
      %li
        =submit_tag "Cancel order"
