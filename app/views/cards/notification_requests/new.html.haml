#message
  %h1 Notification request
  =semantic_form_for @notification_request, :url => card_notification_request_path(@card.code) do |f|
    =f.inputs do
      =f.input :email, :label => 'Email address'
    =f.buttons do
      =f.commit_button "Notify me*"
