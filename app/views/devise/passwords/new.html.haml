%h2 Forgot your password?
=semantic_form_for resource, :url => account_password_forgot_path do |f|
  =f.inputs do
    =f.input :email
  =f.buttons do
    =f.commit_button "Send me reset password instructions"
