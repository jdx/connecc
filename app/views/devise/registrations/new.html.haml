%h2 Sign up
=semantic_form_for resource, :url => signup_path do |f|
  =f.inputs do
    =f.input :first_name
    =f.input :last_name
    =f.input :email
    =f.input :password
  =f.buttons do
    =f.commit_button "Sign up"
