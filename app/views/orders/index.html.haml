-content_for :title do
  Orders
%h1 Your Orders
%hr
%table
  %tr
    %th Order
    %th Type
    %th State
    %th Placed
  -for order in @orders
    %tr
      %td= link_to order, order_path(order)
      %td= order.type
      %td= order.state
      %td= order.created_at.to_s(:short)
